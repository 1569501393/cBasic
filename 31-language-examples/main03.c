#include <stdio.h>

int main() {
    /* C 语言实例 - 输出单个字符 */
    char c;
    c = 'A';
    printf("字符 %c 的 ASCII 值是 %d\n", c, c);

    c = 'a';
    printf("字符 %c 的 ASCII 值是 %d\n", c, c);

    return 0;
}
